## Mesher Solidity

Add the relevent environmrnt Variables in the `application.prperties` file.

To run mesher for Hyperledger Besu:
```
SPRING_PROFILES_ACTIVE=hl-besu ./gradlew boolrun
```

To run mesher for Hyperledger Fabric:
```
SPRING_PROFILES_ACTIVE=hl-fabric ./gradlew boolrun
```